{
  "name": "solana-counter-program",
  "version": "0.1.0",
  "description": "Anchor-based Solana counter program with frontend",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy": "./scripts/deploy.sh",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.91.4"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "prettier": "^3.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.2.2"
  }
}
